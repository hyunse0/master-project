"""목록형 질의(개별 레코드 나열) 전용 프롬프트 가이드."""

GUIDANCE = """\
- 이 질문은 목록형입니다. 집계 함수(COUNT/SUM/AVG 등)나 GROUP BY를 임의로 추가하지 마세요.
- WHERE 조건으로 명시된 필터를 정확히 반영하고, 불필요한 조인으로 행이 중복 확장(fan-out)
  되지 않게 하세요.
- 정렬 기준이 질문에 없으면 가장 자연스러운 식별자(예: 날짜, ID)로 ORDER BY 하세요.
"""
