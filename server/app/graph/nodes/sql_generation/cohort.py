"""코호트형 질의(여러 포함/제외 조건으로 대상군을 정의) 전용 프롬프트 가이드."""

GUIDANCE = """\
- 이 질문은 코호트 정의형입니다. 조건이 여러 개면 한 SELECT에 다 우겨넣지 말고 CTE(WITH)로
  단계별 필터링 후 마지막에 결합하세요 — 각 조건이 정확히 반영됐는지 검증하기 쉬워집니다.
- 1:N 관계 테이블(예: 진단/처방 이력)과 조인할 때는 반환 대상(예: 환자)이 중복되지 않도록
  EXISTS 서브쿼리 또는 사전 집계된 CTE를 사용하세요 — 단순 JOIN은 행이 fan-out되어 대상 수가
  과대 계산될 위험이 큽니다.
- 포함/제외 조건은 [값 위치] 근거 없이 임의로 만들지 말고, 각 조건이 어떤 컬럼/값에 근거하는지
  SchemaCitation에 모두 명시하세요.
"""
