"""집계형 질의(건수·합계·평균·분포 등) 전용 프롬프트 가이드."""

GUIDANCE = """\
- 이 질문은 집계형입니다. GROUP BY 없이 여러 행을 반환하는 SELECT를 쓰지 마세요.
- SELECT 절의 비집계 컬럼은 반드시 GROUP BY에 포함하세요.
- 지표가 비율/평균이면 분모가 0이 되는 경우를 NULLIF로 방지하세요.
- 그룹 개수가 많을 수 있는 dimension은 집계값 기준 ORDER BY DESC로 정렬해 상위값이 먼저
  보이게 하세요.
"""
